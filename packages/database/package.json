{
  "name": "@boat-monitor/database",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --build && cp -r src/migrations dist/",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit",
    "migrate": "node dist/migrate.js",
    "migrate:create": "node scripts/create-migration.js"
  },
  "dependencies": {
    "@boat-monitor/shared": "*",
    "dotenv": "^17.2.3",
    "drizzle-orm": "^0.38.3",
    "postgres": "^3.4.5",
    "uuid": "^11.0.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/uuid": "^10.0.0",
    "drizzle-kit": "^0.30.1",
    "typescript": "^5.7.2"
  }
}
